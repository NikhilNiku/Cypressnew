name: Cypress Demp
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: windows-latest
    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - run: npm install
      - run: npx cypress run --spec "cypress/integration/AutoCheckBillingTests/Running.spec.js
